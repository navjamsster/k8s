kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
    extraPortMappings:
      - containerPort: 30080   # NodePort on worker
        hostPort: 30080        # Port on your laptop/host
        protocol: TCP
        